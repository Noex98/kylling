# Required. Vercel sets this automatically once a Blob store is connected to
# the project; run `vercel env pull .env.local` to get it locally.
#
# Note that local and deployed then share the same state — ticking a bar while
# testing is visible to everyone in the game.
BLOB_READ_WRITE_TOKEN=
