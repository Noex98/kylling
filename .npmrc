# Supply-chain guard: never install a package version published less than 3
# days ago. Value is in days. Set at project level too, so the policy travels
# with the repo (CI, other machines) instead of relying on a personal ~/.npmrc.
min-release-age=3
